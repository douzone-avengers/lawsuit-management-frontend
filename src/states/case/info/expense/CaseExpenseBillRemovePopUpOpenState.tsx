import { atom } from "recoil";

const caseExpenseBillRemovePopUpOpenState = atom({
  key: "caseExpenseBillRemovePopUpOpenState",
  default: false,
});

export default caseExpenseBillRemovePopUpOpenState;
