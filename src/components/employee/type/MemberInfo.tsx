import { Hierarchy, Role } from "../../../states/common/IsLoginState";

export type MemberInfo = {
  id: number;
  email: string;
  name: string;
  phone: string;
  address: string;
  hierarchy: string;
  role: string;
};
